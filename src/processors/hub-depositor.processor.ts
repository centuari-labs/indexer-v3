import type { EventProcessor } from "../core/event-dispatcher.js";

/** Stub — Step 11. Emits `Deposit` / `Payout` → `deposit_event` rows. */
export const hubDepositorProcessors: EventProcessor[] = [];
