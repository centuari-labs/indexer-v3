import type { EventProcessor } from "../core/event-dispatcher.js";

/** Stub — Step 11. Spoke-side custody accounting for SPOKE_NATIVE assets. */
export const spokeVaultProcessors: EventProcessor[] = [];
