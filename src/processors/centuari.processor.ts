import type { EventProcessor } from "../core/event-dispatcher.js";

/**
 * Decode-only Phase 1 stub. Full implementation lands in Step 11 once
 * Order / Match / Repay / BondMint event shapes are final in the ABI.
 */
export const centuariProcessors: EventProcessor[] = [];
