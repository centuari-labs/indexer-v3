import type { EventProcessor } from "../core/event-dispatcher.js";

/**
 * Phase 1 dormant — solver reimbursement flow not active yet.
 * Decode-only stub kept for forward compat.
 */
export const settlementLedgerProcessors: EventProcessor[] = [];
